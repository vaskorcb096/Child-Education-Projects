import React from 'react';

const AdminPendingContact = ({props}) => {
    console.log(props);
    
    return (
        <div className="text-success text-center">
        </div>
    );
};

export default AdminPendingContact;